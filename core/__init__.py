"""Backend modules: hardware/system integration, independent of HTTP layer.

    kwin           — KWin scripting (KDE Plasma)
    procs          — /proc-based process index
    notifications  — DBus notification listener
    intel_gpu      — intel_gpu_top streaming parser
    audio          — Volume + per-app streams
    displays       — Brightness (KDE PM + DDC/CI)
    system         — Power profiles, DND, battery, sysinfo
    cachy          — arch-update tray integration
"""
